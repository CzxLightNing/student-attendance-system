# 项目设计

## 1. 数据模型

> 数据模型是所需数据结构的概念表示

## 2. 用户界面设计

> 展示为可视化用户浏览时与系统交互而构建的用户界面设计（请使用截图）

## 3. 实现技术

> 在您的项目中，您必须创建一个包含以下功能的网站：

a. 至少 3 个页面  
b. 数据库集成  
c. 注册和登录表单  
d. 使用 Django 模板语言（Jinja）  
e. 在项目中创建至少 2 个应用  
f. 为所有页面实现 Bootstrap 样式  
g. 每个页面必须包含页眉、主体和页脚  
h. 页眉必须包含所有导航部分  
i. 页脚必须包含您的联系信息  
j. 授权用户必须能够通过网站表单向数据库添加信息  
k. 您必须实现基于类的视图  

## 4. 您的项目展示

> 介绍项目本身，展示您使用的一些代码，展示一些数据表并展示流程本身

---

# Project Design

## 1. Data Model

> A data model is a conceptual representation of the data structures that are required

## 2. User Interface Design

> Show the user interface designs that were constructed to visualize the user interaction with the system as they browse (use screenshots)

## 3. Implementation Technologies

> In your project you must create a website with:

a. At least 3 pages  
b. Database integration  
c. Registration and login forms  
d. Use Django Template Language (Jinja)  
e. Create at least 2 applications inside of your project  
f. Implement Bootstrap style to all your pages  
g. Each page must contain header, body and footer  
h. Header must do all navigation part  
i. Footer must contain contact information about you  
j. Authorized users must be able to add information into the database through the site forms  
k. You must implement class-based views  

## 4. Your Project Presentation

> Tell about the project itself, show some codes you used, show some tables and show the processes itself